package com.dogs.window.impl;

import com.dogs.window.Window;
import com.sun.jna.platform.win32.WinDef;

public class LoginWindow extends Window {

    public LoginWindow(WinDef.HWND reference) {
        super(reference);
    }
}
